name: CI
on: [push]
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: borales/actions-yarn@v2.3.0  # will run `yarn`
      - uses: borales/actions-yarn@v2.3.0
        with:
          cmd: build # will run `yarn build` command
